import { AppRoutes } from './app/AppRoutes';

/**
 * Root application component.
 * Routing is kept in its own module so this file remains intentionally small.
 */
export default function App() {
  return <AppRoutes />;
}
